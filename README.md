# MemoQuiz Forge

Outil local de préparation, contrôle et export de questions pour MemoQuiz.

## Initialiser la base locale

```bash
python -m memoquiz_forge init
```

La commande crée la base SQLite dans `data/memoquiz-forge.db`.
